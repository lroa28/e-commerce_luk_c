<div>
    <a href="/" class="btn btn-primary rounded mb-4 mll-4 px-3 py-2"> Volver al formulario</a>
</div>
<div class="container m-5">
    <div class="mt-5">
        <div class="table-responsive mt- rounded-lg">
            <h4 class="text-danger">Vista de Productos:</h4>
            {{#if productos}}
            <table class="table table-dark rounded-lg">
                <thead>
                    <tr>
                        <th>Nombre:</th>
                        <th>Precio:</th>
                        <th>Foto:</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each productos }}
                    <tr>
                        <td class="title"> {{ this.title }} </td>
                        <td class="price"> $ {{ this.price }} </td>
                        <td class="thumbnail"> <img src=" {{ this.thumbnail }}">
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            {{else}}
            <h5 class="alert alert-danger">No se encontraron productos.</h5>
            {{/if}}
        </div>
    </div>
</div>