{{!-- main.hbs --}}
{{> lovedChamps}}
<br>
{{> hatedChamps}}
<br>

<h2>List of my most suggested League Champions</h2>

{{#if listExists}}
    <ul>
        {{#each suggestedChamps}} <!--if, unless, each y with, son metodos p handlebars -->
            <li class="{{this.lane}}">{{this.name}}</li>
        {{/each}}
    </ul>
{{/if}}